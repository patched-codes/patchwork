[tool.poetry.group.dev.dependencies]
setuptools = "*"
poethepoet = { version = "^0.24.2", extras = ["poetry-plugin"] }
mypy = "^1.7.1"
types-requests = "~2.31.0"
black = "^24.3.0"  # Updated version to fix vulnerability
isort = "^5.13.2"
autoflake = "^2.3.1"
pytest = "^8.1.1"